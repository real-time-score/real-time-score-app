# 경기 데이터 관리 (Match Management)

## 1. 개요

이 문서는 스포츠스코어픽 Admin에서 경기 관련 데이터(종목 카테고리, 경기 일정, 배당 정보, 예측게임 항목)를 관리하는 기능을 정의합니다.

- **원본 PDF 참조**: 13페이지 (종목 카테고리), 12-19페이지 (경기 리스트), 35페이지 (예측게임)
- **적용 범위**: Admin > 경기 관리 메뉴

---

## 2. 종목 카테고리 관리

### 2.1 종목 구조

PDF 13페이지, 46페이지 참조:

```
종목 (Sport)
├── 축구 (Soccer)
│   ├── 프리미어리그
│   ├── 라리가
│   ├── 분데스리가
│   ├── 세리에A
│   └── K리그
├── 야구 (Baseball)
│   ├── KBO
│   ├── MLB
│   └── NPB
├── 농구 (Basketball)
│   ├── KBL
│   ├── NBA
│   └── WNBA
├── 배구 (Volleyball)
│   ├── V리그 남자
│   └── V리그 여자
├── 하키 (Hockey)
│   └── NHL
└── E스포츠 (Esports)
    ├── LoL
    └── 발로란트
```

### 2.2 종목 목록 화면

```
┌─────────────────────────────────────────────────────────────┐
│ 종목 카테고리 관리                            [+ 종목 등록]  │
├─────────────────────────────────────────────────────────────┤
│ ID │ 아이콘 │ 종목명 │ 리그수 │ 상태 │ 순서 │ 관리          │
├────┼────────┼────────┼────────┼──────┼──────┼───────────────┤
│ 1  │ ⚽     │ 축구   │ 15     │ 활성 │ 1    │ [수정][리그]  │
│ 2  │ ⚾     │ 야구   │ 5      │ 활성 │ 2    │ [수정][리그]  │
│ 3  │ 🏀     │ 농구   │ 4      │ 활성 │ 3    │ [수정][리그]  │
│ ...                                                         │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 종목 등록/수정 화면

| 필드 | 타입 | 필수 | 설명 |
|------|------|:----:|------|
| 종목명 | Text | ✅ | 한글 종목명 |
| 영문명 | Text | ✅ | 영문 종목명 |
| 아이콘 | Image | ✅ | 종목 아이콘 |
| 상태 | Toggle | ✅ | 활성/비활성 |
| 노출 순서 | Number | ✅ | 정렬 순서 |

### 2.4 종목 데이터 구조

```typescript
interface Sport {
  id: number;
  name: string;              // 한글명
  nameEn: string;            // 영문명
  iconUrl: string;
  isActive: boolean;
  sortOrder: number;
  leagueCount: number;       // 하위 리그 수
  createdAt: Date;
  updatedAt: Date;
}
```

---

## 3. 리그/대회 관리

### 3.1 리그 목록 화면

```
┌─────────────────────────────────────────────────────────────┐
│ 리그 관리 - 축구                              [+ 리그 등록]  │
├─────────────────────────────────────────────────────────────┤
│ [상태 ▼] [검색어_____] [검색]                               │
├─────────────────────────────────────────────────────────────┤
│ ID │ 로고 │ 리그명 │ 국가 │ 시즌 │ 경기수 │ 상태 │ 순서 │관리│
├────┼──────┼────────┼──────┼──────┼────────┼──────┼──────┼────┤
│ 1  │[img] │ 프리미어│ ENG  │24/25 │ 380    │ 활성 │ 1    │[수정]│
│ 2  │[img] │ 라리가 │ ESP  │24/25 │ 380    │ 활성 │ 2    │[수정]│
│ ...                                                         │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 리그 등록/수정 화면

| 필드 | 타입 | 필수 | 설명 |
|------|------|:----:|------|
| 종목 | Select | ✅ | 상위 종목 |
| 리그명 | Text | ✅ | 한글 리그명 |
| 영문명 | Text | ✅ | 영문 리그명 |
| 국가 | Select | ✅ | 국가 코드 |
| 로고 | Image | - | 리그 로고 |
| 현재 시즌 | Text | ✅ | ex) 2024/25 |
| 상태 | Toggle | ✅ | 활성/비활성 |
| 노출 순서 | Number | ✅ | 정렬 순서 |

### 3.3 리그 데이터 구조

```typescript
interface League {
  id: number;
  sportId: number;
  name: string;
  nameEn: string;
  country: string;           // 국가 코드
  logoUrl?: string;
  currentSeason: string;
  isActive: boolean;
  sortOrder: number;
  matchCount: number;        // 현재 시즌 경기 수
  createdAt: Date;
  updatedAt: Date;
}
```

---

## 4. 경기 일정 관리

### 4.1 경기 데이터 소스

| 소스 | 설명 |
|------|------|
| API 연동 | 외부 데이터 제공 업체 API [확인 필요] |
| 수동 등록 | 관리자 직접 등록 |

### 4.2 경기 목록 화면

```
┌─────────────────────────────────────────────────────────────┐
│ 경기 일정 관리                                [+ 경기 등록]  │
├─────────────────────────────────────────────────────────────┤
│ [종목 ▼] [리그 ▼] [날짜: 2025-12-22 ▼] [상태 ▼] [검색]     │
├─────────────────────────────────────────────────────────────┤
│ ID │ 리그 │ 홈팀 │ 스코어 │ 원정팀 │ 시간 │ 상태 │ 관리    │
├────┼──────┼──────┼────────┼────────┼──────┼──────┼─────────┤
│ 1  │ EPL  │맨유  │ 2 : 1  │ 첼시   │ 22:00│ 종료 │[상세]   │
│ 2  │ EPL  │아스널│ - : -  │ 토트넘 │ 00:30│ 예정 │[수정]   │
│ ...                                                         │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 경기 상태

| 상태 | 설명 |
|------|------|
| 예정 | 경기 시작 전 |
| 진행중 | 경기 진행 중 |
| 휴식 | 하프타임 등 |
| 종료 | 경기 종료 |
| 연기 | 경기 연기 |
| 취소 | 경기 취소 |

### 4.4 경기 등록/수정 화면

| 필드 | 타입 | 필수 | 설명 |
|------|------|:----:|------|
| 리그 | Select | ✅ | 리그 선택 |
| 홈팀 | Select | ✅ | 홈팀 선택 |
| 원정팀 | Select | ✅ | 원정팀 선택 |
| 경기일시 | DateTime | ✅ | 경기 시작 시간 |
| 경기장 | Text | - | 경기장명 |
| 상태 | Select | ✅ | 경기 상태 |
| 홈스코어 | Number | - | 홈팀 점수 |
| 원정스코어 | Number | - | 원정팀 점수 |

### 4.5 경기 데이터 구조

```typescript
interface Match {
  id: number;
  externalId?: string;       // 외부 API ID
  leagueId: number;
  homeTeamId: number;
  awayTeamId: number;
  matchTime: Date;
  venue?: string;
  status: 'scheduled' | 'live' | 'halftime' | 'finished' | 'postponed' | 'cancelled';
  homeScore?: number;
  awayScore?: number;

  // 상세 정보
  homeHalfScore?: number;
  awayHalfScore?: number;
  attendance?: number;

  createdAt: Date;
  updatedAt: Date;
}
```

---

## 5. 팀 관리

### 5.1 팀 목록 화면

```
┌─────────────────────────────────────────────────────────────┐
│ 팀 관리                                        [+ 팀 등록]   │
├─────────────────────────────────────────────────────────────┤
│ [종목 ▼] [리그 ▼] [검색어_____] [검색]                      │
├─────────────────────────────────────────────────────────────┤
│ ID │ 로고 │ 팀명 │ 리그 │ 홈구장 │ 경기수 │ 상태 │ 관리    │
├────┼──────┼──────┼──────┼────────┼────────┼──────┼─────────┤
│ 1  │[img] │ 맨유 │ EPL  │ 올드트래│ 25    │ 활성 │ [수정]  │
│ 2  │[img] │ 첼시 │ EPL  │ 스탬포드│ 25    │ 활성 │ [수정]  │
│ ...                                                         │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 팀 등록/수정 화면

| 필드 | 타입 | 필수 | 설명 |
|------|------|:----:|------|
| 종목 | Select | ✅ | 종목 선택 |
| 리그 | Select | ✅ | 소속 리그 |
| 팀명 | Text | ✅ | 한글 팀명 |
| 영문명 | Text | ✅ | 영문 팀명 |
| 로고 | Image | ✅ | 팀 로고 |
| 홈구장 | Text | - | 홈 경기장명 |
| 창단연도 | Number | - | 창단 연도 |
| 상태 | Toggle | ✅ | 활성/비활성 |

### 5.3 팀 데이터 구조

```typescript
interface Team {
  id: number;
  externalId?: string;
  sportId: number;
  leagueId: number;
  name: string;
  nameEn: string;
  logoUrl: string;
  venue?: string;
  foundedYear?: number;
  isActive: boolean;
  createdAt: Date;
  updatedAt: Date;
}
```

---

## 6. 배당 정보 관리

### 6.1 배당 유형

PDF 15-19페이지 참조:

| 유형 | 설명 | 적용 종목 |
|------|------|-----------|
| 승무패 | 홈승/무/원정승 | 축구 |
| 승패 | 홈승/원정승 | 야구, 농구 등 |
| 핸디캡 | 핸디캡 적용 승패 | 전 종목 |
| 언더오버 | 총 득점 기준점 | 전 종목 |

### 6.2 배당 목록 화면

```
┌─────────────────────────────────────────────────────────────┐
│ 배당 정보 관리                                               │
├─────────────────────────────────────────────────────────────┤
│ [종목 ▼] [리그 ▼] [날짜: 2025-12-22 ▼]                      │
├─────────────────────────────────────────────────────────────┤
│ 경기: 맨유 vs 첼시 (2025-12-22 22:00)                        │
├─────────────────────────────────────────────────────────────┤
│ 베팅 유형 │ 옵션1 │ 배당1 │ 옵션2 │ 배당2 │ 옵션3 │ 배당3 │ 관리│
├───────────┼───────┼───────┼───────┼───────┼───────┼───────┼────┤
│ 승무패    │ 홈승  │ 2.10  │ 무승부│ 3.25  │ 원정  │ 3.40  │[수정]│
│ 핸디 -1  │ 홈승  │ 2.85  │       │       │ 원정  │ 1.45  │[수정]│
│ 언오버 2.5│ 오버  │ 1.90  │       │       │ 언더  │ 1.90  │[수정]│
└─────────────────────────────────────────────────────────────┘
```

### 6.3 배당 데이터 구조

```typescript
interface Odds {
  id: number;
  matchId: number;
  type: '1x2' | '12' | 'handicap' | 'over_under';

  // 승무패/승패
  homeOdds?: number;
  drawOdds?: number;        // 무승부 (축구)
  awayOdds?: number;

  // 핸디캡
  handicapLine?: number;    // ex) -1.5
  handicapHomeOdds?: number;
  handicapAwayOdds?: number;

  // 언더오버
  overUnderLine?: number;   // ex) 2.5
  overOdds?: number;
  underOdds?: number;

  provider?: string;        // 배당 제공 업체
  updatedAt: Date;
}
```

---

## 7. 예측게임 항목 관리

### 7.1 예측게임 개요

PDF 35페이지 참조:

경기별로 유저들이 예측할 수 있는 게임 항목을 설정

### 7.2 예측게임 유형

| 유형 | 설명 | 예시 |
|------|------|------|
| 승무패 예측 | 경기 결과 예측 | 홈승/무/원정 |
| 스코어 예측 | 정확한 스코어 예측 | 2:1 |
| 득점자 예측 | 첫 득점자/다득점자 | 손흥민 |
| 이벤트 예측 | 특정 이벤트 발생 | 양팀 득점 |

### 7.3 예측게임 항목 목록

```
┌─────────────────────────────────────────────────────────────┐
│ 예측게임 항목 관리                            [+ 항목 등록]  │
├─────────────────────────────────────────────────────────────┤
│ 경기 선택: [맨유 vs 첼시 (12-22 22:00) ▼]                    │
├─────────────────────────────────────────────────────────────┤
│ ID │ 항목명 │ 유형 │ 선택지 │ 참여수 │ 상태 │ 관리          │
├────┼────────┼──────┼────────┼────────┼──────┼───────────────┤
│ 1  │ 승무패 │ 결과 │ 3개    │ 1,234  │ 진행 │ [수정][결과]  │
│ 2  │ 정확한스│ 스코어│ 다수  │ 567    │ 진행 │ [수정][결과]  │
│ 3  │ 첫골  │ 득점자│ 22개  │ 234    │ 종료 │ [결과보기]    │
│ ...                                                         │
└─────────────────────────────────────────────────────────────┘
```

### 7.4 예측게임 항목 등록

| 필드 | 타입 | 필수 | 설명 |
|------|------|:----:|------|
| 경기 | Select | ✅ | 대상 경기 |
| 항목명 | Text | ✅ | 예측 항목 제목 |
| 유형 | Select | ✅ | 예측 유형 |
| 선택지 | Dynamic | ✅ | 예측 옵션들 |
| 지급 포인트 | Number | ✅ | 정답 시 포인트 |
| 마감 시간 | DateTime | ✅ | 예측 마감 시간 |
| 상태 | Select | ✅ | 대기/진행/종료 |

### 7.5 예측게임 결과 처리

```
┌─────────────────────────────────────────────────────────────┐
│                   예측게임 결과 처리                         │
├─────────────────────────────────────────────────────────────┤
│ 경기: 맨유 2:1 첼시                                          │
│ 항목: 승무패 예측                                            │
│                                                             │
│ 정답 선택:                                                   │
│ ● 홈승 (맨유)  ○ 무승부  ○ 원정승 (첼시)                    │
│                                                             │
│ 참여 현황:                                                   │
│ - 홈승: 523명 (42.4%)                                       │
│ - 무승부: 312명 (25.3%)                                     │
│ - 원정승: 399명 (32.3%)                                     │
│                                                             │
│ 정답자 수: 523명                                             │
│ 지급 예정 포인트: 52,300P                                    │
│                                                             │
│          [취소]              [결과 확정]                    │
└─────────────────────────────────────────────────────────────┘
```

### 7.6 예측게임 데이터 구조

```typescript
interface PredictionGame {
  id: number;
  matchId: number;
  title: string;
  type: 'result' | 'score' | 'scorer' | 'event';
  options: PredictionOption[];
  rewardPoints: number;
  deadline: Date;
  status: 'pending' | 'active' | 'closed' | 'settled';
  correctOptionId?: number;
  participantCount: number;
  createdAt: Date;
  updatedAt: Date;
}

interface PredictionOption {
  id: number;
  gameId: number;
  label: string;
  value: string;
  participantCount: number;
}

interface UserPrediction {
  id: number;
  userId: number;
  gameId: number;
  optionId: number;
  isCorrect?: boolean;
  pointsAwarded?: number;
  createdAt: Date;
}
```

---

## 8. API 요구사항

### 8.1 종목/리그 관리 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | /api/admin/sports | 종목 목록 조회 |
| POST | /api/admin/sports | 종목 등록 |
| PUT | /api/admin/sports/{id} | 종목 수정 |
| DELETE | /api/admin/sports/{id} | 종목 삭제 |
| GET | /api/admin/leagues | 리그 목록 조회 |
| POST | /api/admin/leagues | 리그 등록 |
| PUT | /api/admin/leagues/{id} | 리그 수정 |
| DELETE | /api/admin/leagues/{id} | 리그 삭제 |

### 8.2 경기 관리 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | /api/admin/matches | 경기 목록 조회 |
| GET | /api/admin/matches/{id} | 경기 상세 조회 |
| POST | /api/admin/matches | 경기 등록 |
| PUT | /api/admin/matches/{id} | 경기 수정 |
| DELETE | /api/admin/matches/{id} | 경기 삭제 |
| POST | /api/admin/matches/sync | 외부 API 동기화 |

### 8.3 팀 관리 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | /api/admin/teams | 팀 목록 조회 |
| GET | /api/admin/teams/{id} | 팀 상세 조회 |
| POST | /api/admin/teams | 팀 등록 |
| PUT | /api/admin/teams/{id} | 팀 수정 |
| DELETE | /api/admin/teams/{id} | 팀 삭제 |

### 8.4 배당 관리 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | /api/admin/matches/{id}/odds | 배당 조회 |
| PUT | /api/admin/odds/{id} | 배당 수정 |
| POST | /api/admin/odds/sync | 배당 동기화 |

### 8.5 예측게임 관리 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | /api/admin/predictions | 예측게임 목록 |
| GET | /api/admin/predictions/{id} | 예측게임 상세 |
| POST | /api/admin/predictions | 예측게임 등록 |
| PUT | /api/admin/predictions/{id} | 예측게임 수정 |
| DELETE | /api/admin/predictions/{id} | 예측게임 삭제 |
| POST | /api/admin/predictions/{id}/settle | 결과 처리 |
| GET | /api/admin/predictions/{id}/participants | 참여자 목록 |

---

## 9. 외부 데이터 연동

### 9.1 연동 대상

| 항목 | 연동 여부 | 비고 |
|------|:---------:|------|
| 경기 일정 | ✅ | 외부 API |
| 실시간 스코어 | ✅ | 외부 API |
| 배당 정보 | ✅ | 외부 API |
| 팀/선수 정보 | ✅ | 외부 API |
| 라인업 | ✅ | 외부 API |
| 통계 | ✅ | 외부 API |

### 9.2 데이터 동기화

| 기능 | 설명 |
|------|------|
| 자동 동기화 | 스케줄러 기반 주기적 동기화 |
| 수동 동기화 | 관리자 버튼 클릭 시 즉시 동기화 |
| 동기화 로그 | 동기화 결과 및 에러 로그 |

### 9.3 동기화 관리 화면

```
┌─────────────────────────────────────────────────────────────┐
│ 데이터 동기화 관리                                           │
├─────────────────────────────────────────────────────────────┤
│ 항목 │ 마지막 동기화 │ 상태 │ 다음 예정 │ 관리              │
├──────┼──────────────┼──────┼──────────┼───────────────────┤
│ 경기일정│ 12-22 10:00 │ 성공 │ 12-22 11:00│ [동기화][로그]  │
│ 실시간점수│ 12-22 10:30│ 성공 │ 1분 후    │ [동기화][로그]  │
│ 배당정보│ 12-22 10:15 │ 실패 │ 12-22 10:30│ [동기화][로그]  │
│ ...                                                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 10. 관리자 기능 체크리스트

### 10.1 종목/리그 관리
- [ ] 종목 목록 조회
- [ ] 종목 CRUD
- [ ] 종목 순서 변경
- [ ] 리그 목록 조회
- [ ] 리그 CRUD
- [ ] 리그 순서 변경

### 10.2 경기 관리
- [ ] 경기 목록 조회 (필터: 종목, 리그, 날짜, 상태)
- [ ] 경기 상세 조회
- [ ] 경기 수동 등록
- [ ] 경기 정보 수정
- [ ] 경기 삭제/취소 처리

### 10.3 팀 관리
- [ ] 팀 목록 조회
- [ ] 팀 CRUD
- [ ] 팀 로고 업로드

### 10.4 배당 관리
- [ ] 배당 정보 조회
- [ ] 배당 수동 수정
- [ ] 배당 동기화

### 10.5 예측게임 관리
- [ ] 예측게임 목록 조회
- [ ] 예측게임 등록
- [ ] 예측게임 수정/삭제
- [ ] 결과 처리 (정답 설정, 포인트 지급)
- [ ] 참여자 목록 조회

### 10.6 데이터 연동
- [ ] 수동 동기화 실행
- [ ] 동기화 로그 조회
- [ ] 동기화 스케줄 설정

---

## 11. 버전 관리

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| v0.1 | 2025-12-22 | 초기 작성 |
